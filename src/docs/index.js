const docsUsers = require("./users.docs.json");
const docsCourses = require("./courses.docs.json");
const docsVideos = require("./videos.docs.json")
const docsCategories = require("./categories.docs.json")

module.exports = {
  docsUsers,
  docsCourses,
  docsVideos,
  docsCategories,
}